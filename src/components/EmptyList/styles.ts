import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  emptyList: {
    flex: 1,
    alignItems: 'center',
    marginTop: 48
  },

  clipboard: {
    width: 56,
    height: 56,
    marginBottom: 16
  },

  emptyListTextBold: {
    color: '#808080',
    fontWeight: 'bold',
    paddingBottom: 4
  },

  emptyListNormal: {
    color: '#808080',
  }
})